<%= simple_form_for([:admins, @industry]) do |f|%>
	<%= f.input :title %>
	<%= f.input :city_id,  collection: City.all.map { |c| [c.name, c.id]}, input_html: {class: "select2"} ,require:'true'%>
	<%= f.submit "Save", class: 'btn btn-devise' %>
<% end %>